Replace the previous note: "No external dependencies (uses built-in Node.js modules)" with the following:

This project requires the `fast-xml-parser` dependency and is intended to run with Node.js version 20 or higher. Please ensure you install the necessary dependencies using `npm ci`.